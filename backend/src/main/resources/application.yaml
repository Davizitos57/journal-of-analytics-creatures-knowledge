spring:
  application:
    name: jack
  
  datasource:
    url: jdbc:postgresql://localhost:5432/jackdb
    username: postgres
    password: Sc4rFox57
    driver-class-name: org.postgresql.Driver

  jpa:
    show-sql: true
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect